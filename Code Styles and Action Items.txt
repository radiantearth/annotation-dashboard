Agreement on style and conventions of code


define functions, and then have module.exports at the end of the file
don't use _fnName for private functions.  just keep them named normally
use `debug` for debugging, eg
  ```const debug = require("debug")("re:services:wallet");```
Promises - catching errors - catch when you can add more info, otw let it bubble up to the outer most caller, eg the /api/ route

To do's

figure out db migrations, eg adding a new field or index
Explore how to properly fail and don not save info about organization if some part of this process fails